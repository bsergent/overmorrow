import Entity from 'overmorrow/classes/Entity';
import Renderer from 'overmorrow/Renderer';

export default class EntityPlayer {
	public draw(ui: Renderer): void {

  }
	public tick(delta: number): void {

  }
}

export class Target {
  // Entity or Position
}